# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=30d  # Token expires in 30 days (was 7d)

# URLs
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000

# Google OAuth (OPTIONAL - Leave empty to disable OAuth)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Exchange Rate API (for currency conversion)
EXCHANGE_RATE_API_KEY=0fe9acb002e50ab852947697

# Email Configuration (OPTIONAL - for password reset emails)

# Option 1: SendGrid (Recommended - 100 emails/day free)
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=your-verified-email@domain.com

# Option 2: Gmail (Alternative)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-gmail-app-password

